#include<stdio.h>
#include<conio.h>
int main(int n)
{
  int i,j,count=0;
  for(i=2;i<n;i++)
  {
    for(j=2;j<i;j++)
    {
      if(i%j==0)
      break;
    }
    if(i==j)
    count++;
  }
  return count;
}
