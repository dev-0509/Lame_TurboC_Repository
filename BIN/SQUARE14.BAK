#include<stdio.h>
#include<conio.h>
void main()
{
  int a,i,j,k=1,l,n;
  clrscr();
  printf("\n\tEnter the number of rows : ");
  scanf("%d",&n);
  a=n;
  for(i=0;i<n;i++)
  {
    for(l=1;l<=a;l++)
    {
      printf("   ");
    }
    for(j=1;j<=((2*i)+1);j++)
    {
      printf(" %d ",k*k);
      k++;
    }
    a--;
    printf("\n\n");
  }
  getch();
}